## Exemplo de variáveis de ambiente para o coletor eTrac
# Credenciais da API eTrac (autenticação Basic Auth)
ETRAC_USER=SEU-EMAIL
ETRAC_KEY=INSERIR-KEY

# Endereço base da API (opcional)
#ETRAC_API_BASE=http://api.etrac.com.br/monitoramento

# Schema onde as tabelas serão criadas/consultadas (padrão: e_track)
ETRAC_SCHEMA=e_track

# Postgres (opcionalmente use DATABASE_URL)
#DATABASE_URL=postgresql://user:password@localhost:5432/dbname
PGHOST=localhost
PGPORT=5432
PGDATABASE=your_db
PGUSER=your_user
PGPASSWORD=your_password
