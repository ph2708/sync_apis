# Centralized configuration example for the repository
# Copy to .env and fill real values (do NOT commit .env)

PGHOST=127.0.0.1
PGPORT=5432
PGDATABASE=sync_apis
PGUSER=sync_user
PGPASSWORD=sync_pass

# Auvo API credentials
AUVO_API_KEY=your_auvo_api_key_here
AUVO_API_TOKEN=your_auvo_api_token_here

# e-Track API credentials
ETRAC_USER=your_etrac_user_here
ETRAC_KEY=your_etrac_key_here
ETRAC_API_BASE=https://api.etrac.com.br/monitoramento
